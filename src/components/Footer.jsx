import React from "react";

const Footer = () => (
  <footer className="footer">
    <span>
      &copy; {new Date().getFullYear()} React URL Shortener. Made with ❤️.
    </span>
  </footer>
);

export default Footer;
